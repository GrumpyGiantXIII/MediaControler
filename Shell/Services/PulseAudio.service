[Unit]
Description=Play audio with pulseaudio no logged on user
After=network.target

[Service]
Type=notify
Exec=/usr/bin/pulseaudio --daemonize=no --system --realtime --log-target=journal

[Install]
WantedBy=multi-user.target
